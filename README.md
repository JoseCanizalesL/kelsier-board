# Kelsier's Board

This is a project manager. Create projects and add task to it. Mark the task as completed to keep track of what you have accomplish.

## React

This project uses React to render componentes and track state hooks.

`npm run dev` to initialize a development server.
